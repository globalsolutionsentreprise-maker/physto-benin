# Lessons — Journal d'apprentissage

Ce fichier est lu au début de chaque session par Claude Code et avant toute modification de code.

## Format

[YYYY-MM-DD] | ce qui s'est mal passé | règle à suivre la prochaine fois

## Entrées

[2026-05-25] | Bouton "Générer certificat" faisait save + print en même temps, sans confirmation visuelle — l'utilisateur perdait son travail si la popup était bloquée | Toujours séparer Sauvegarder (garde le modal ouvert, confirme en vert) et Imprimer (ouvre seulement la fenêtre d'impression)

[2026-05-25] | Liste documents sans aperçu : impossible de distinguer plusieurs certificats du même client | Toujours ajouter un bouton "Aperçu" dans les listes de documents, régénérant le HTML depuis les données sauvegardées
[2026-05-28] | createClient(@supabase/supabase-js) appelé au niveau module dans analyze-contract, create-client, generate-contract — build échoue avec "supabaseKey is required" car les env vars ne sont pas disponibles à build time | Toujours instancier createClient à l'intérieur du handler (GET/POST), jamais au niveau module ; ajouter export const dynamic = "force-dynamic" sur chaque route API Supabase

[2026-05-29] | L'IA (analyze-contract) ignorait la demande du client sur la fréquence de passages même avec RÈGLE 0 dans le prompt. L'IA utilise son propre jugement et ignore les règles textuelles. | Ne jamais faire confiance à l'IA seule pour respecter une contrainte métier critique. Toujours parser la valeur dans le code JS (parseFrequenceClient), l'injecter comme contrainte dure dans le prompt ET l'écraser dans la réponse après le JSON.parse. Double sécurité : prompt + surcharge côté serveur.

[2026-05-29] | Le bouton "Générer directement" (prix négocié) ignorait demandeClient et hardcodait passages=4/paiement=trimestriel. L'utilisateur tape "deux passages par an" mais le contrat générait 4 passages. | Toujours parser demandeClient dans TOUS les chemins de génération (IA et direct). Dupliquer la même regex de parsing côté frontend pour le bouton direct.
[2026-06-02] | Activation RLS sans policies brise le portail client (espace-client utilise la clé anon + JWT) et l'admin panel (accès CMS direct via anon) | Toujours créer les policies RLS en même temps que l'activation RLS — jamais ALTER TABLE ENABLE ROW LEVEL SECURITY sans la migration de policies correspondante

[2026-06-04] | Bloc prix-par-prestation ne s'affichait pas avec condition `length > 1` — le cas 1 prestation était exclu, et les devis existants rechargés depuis la BDD avaient parfois 1 seule prestation dans l'array même si plusieurs étaient visibles | Utiliser `length >= 1` dès qu'il y a des prestations sélectionnées ; le bloc multi-prestation doit s'afficher pour N >= 1 prestations

[2026-06-04] | Modifier le nom dans la fiche CRM (crm.html) ne mettait pas à jour le rapport de visite — le champ `client` (nom) envoyé par `saveClient()` était ignoré dans le destructuring de `save_client` dans crm-data/route.js ; la table `clients.nom` n'était jamais mise à jour | Dans `save_client`, toujours déstructurer et persister le champ `client` vers `clients.nom`. Attention : crm.html et app/admin/page.js sont deux systèmes distincts qui partagent la même BDD — un changement dans l'un peut ne pas se propager à l'autre si l'API n'est pas complète.

[2026-06-02] | Audit de sécurité CSO a révélé 5 vulnérabilités sur le site en prod : (1) bypass admin via localStorage.setItem("phyto-benin_admin_v4","oui"), (2) mot de passe hardcodé "phyto-benin2025" visible dans le bundle JS, (3) routes /api/crm-data et /api/rh-data sans auth retournaient toutes les données business publiquement, (4) TLS désactivé (rejectUnauthorized:false) sur les appels FedaPay, (5) RLS activé sans policies cassait l'espace-client | Corrections appliquées et vérifiées : supprimer tout fallback localStorage dans l'auth admin ; ne jamais hardcoder de mot de passe dans le code client ; toujours vérifier le token Supabase (Authorization: Bearer) dans les routes API admin ; ne jamais désactiver rejectUnauthorized en prod ; créer les policies RLS avec la migration d'activation. Vérification post-fix : build propre, /api/crm-data → 401 sans token, toutes les pages publiques → 200, bundle prod sans traces des vulnérabilités.

[2026-06-04] | Technicien rapport de visite non auto-rempli, et aucun lien vers le planning | 1) Auto-remplir technicien si personnelAdmin.length === 1 dans ouvrirNouveauRapportVisite ; 2) À la sauvegarde d'un nouveau rapport de visite (pas editingId), si technicien + dateVisite renseignés et aucune intervention existante pour ce devis_id, créer automatiquement une entrée "planifiee" dans la table interventions via /api/rh-data avec le token Supabase de la session courante

[2026-06-09] | CRM add_client : le toast "Client ajouté" s'affichait mais le client n'apparaissait pas — triple bug : (1) toast hors du try-catch → success apparent même en cas d'erreur, (2) generate_devis_numero RPC retourne un doublon existant → constraint 23505, (3) fallback Date.now().slice(-6) cyclique toutes les 16 min → doublon après tests répétés | Ne jamais appeler generate_devis_numero sans fallback garanti unique. Utiliser crypto.randomUUID() directement sans RPC. Toujours mettre le toast DANS le bloc de succès. Exposer l'erreur API dans la réponse (pas juste console.error) pour diagnostiquer rapidement.

[2026-06-10] | Webhook WhatsApp ne recevait aucun message réel — le code utilisait WHATSAPP_TOKEN (access token) comme clé HMAC pour vérifier les signatures Meta, mais Meta signe les payloads webhook avec l'App Secret. Résultat : tous les appels Meta retournaient 401 silencieusement. Les tests locaux passaient car ils utilisaient le même mauvais token pour générer la signature. | Pour tout webhook Meta/WhatsApp : utiliser une variable dédiée WHATSAPP_APP_SECRET (valeur dans Paramètres app → Général → Clé secrète) pour la vérification HMAC — jamais l'access token. Ce sont deux secrets distincts : access token = authentification API sortante, App Secret = vérification signature webhook entrante.

[2026-06-17] | Tables leads + whatsapp_conversations créées sans RLS après la migration RLS globale — alerte sécurité Supabase "rls_disabled_in_public" reçue par email 5 jours plus tard | Toujours ajouter ALTER TABLE … ENABLE ROW LEVEL SECURITY dans chaque migration qui crée une nouvelle table, même si service_role bypass RLS automatiquement — sinon n'importe qui avec l'URL publique peut lire/écrire les données

[2026-06-11] | Pages /services/[slug] toutes en 404 en production malgré un build correct — Next.js 15+ rend `params` asynchrone (Promise). Accéder à `params.slug` sans await retourne undefined, déclenchant notFound() sur toutes les pages SSG. Le build générait bien les 8 fichiers HTML mais avec le contenu de la page d'erreur 404 dedans. | Sur ce projet (Next.js 16+) : toujours typer `params` comme `Promise<{ slug: string }>` dans les pages dynamiques, et `await params` avant toute utilisation. Rendre le composant `async` si ce n'est pas déjà le cas. S'applique à `generateMetadata`, `generateStaticParams` si elle utilise params, et au composant page.

[2026-06-19] | Devis imprimé : (1) ligne supplémentaire "0" dans le tableau prestations — `prestation.split(" + ")` pouvait produire une chaîne vide si trailing " + ", et une prestation sans prix dans `prixParPrestation` générait une ligne "0 FCFA/m²". (2) devis sur 2 pages après mise à jour navigateur — le CSS `@media print` du devis était minimal (pas de réduction de padding/marges), contrairement aux autres documents qui utilisent GSE_DOC_STYLES avec un print CSS complet. | Dans `imprimerDevis` : toujours `.filter(Boolean)` sur `prestList` après split, et `.filter(p => parseFloat(ppp[p]) > 0)` avant de générer les lignes du tableau. Le bloc MTN MoMo doit avoir la classe `momo-block` pour être masqué à l'impression (`display:none`). Le CSS `@media print` du devis doit réduire padding/marges/font-size autant que GSE_DOC_STYLES.

[2026-06-29] | En insérant 4 nouveaux articles de blog dont un marqué `vedette=true`, un ancien article était déjà `vedette=true` en base. La page `/blog` n'affiche qu'UNE vedette (`articlesFiltres.find(a => a.vedette)`) ET exclut tous les articles vedette de la grille (`autres = filter(a => !a.vedette)`). Résultat : le 2e article vedette devenait totalement invisible dans la liste (accessible seulement par URL directe). | Avant d'insérer/mettre à jour un article avec `vedette=true`, toujours repasser les autres articles à `vedette=false` (un seul à la une à la fois). Vérifier après tout insert que `select count(*) from articles where vedette=true` = 1. Penser aussi aux doublons de sujet (deux articles "cafards") qui cannibalisent le SEO.

[2026-06-29] | Bot WhatsApp répondait "Votre demande est bien enregistrée..." à TOUT message (même "bonjour") dès qu'un lead avait été créé pour ce numéro. La condition `if (conv.lead_created)` court-circuitait Gemini et renvoyait un message de fin à vie : tout client déjà venu qui revenait avec un nouveau besoin se faisait jeter → leads perdus. Le numéro de test restait bloqué dans cet état. | Ne jamais transformer le bot en cul-de-sac après un lead. Quand `conv.lead_created` est true et qu'un nouveau message arrive, repartir sur une conversation neuve (reset messages + lead_created=false) au lieu de stonewaller. Sûr car /api/register-lead dédoublonne par téléphone sur 24h. Un client qui réécrit = nouveau besoin potentiel, jamais une impasse.

[2026-06-24] | Bot WhatsApp silencieux depuis 1 semaine — aucun appel sur /api/whatsapp-webhook dans les logs Vercel. Le webhook Meta s'était désabonné silencieusement, probablement après les erreurs HMAC répétées du bug corrigé le 2026-06-10. Meta désactive automatiquement un webhook qui accumule des erreurs. Le code, les variables Vercel et le numéro (+229 53047950, statut CONNECTED, qualité GREEN) étaient tous intacts. | Quand le bot WhatsApp ne répond plus : (1) vérifier les logs Vercel sur /api/whatsapp-webhook — zéro appel = Meta n'envoie plus rien ; (2) aller dans Meta Developer Console → app Phyto Bénin → WhatsApp → Configuration → vérifier que l'URL de rappel est configurée et que le champ `messages` est Abonné(e) ; (3) si le webhook est désabonné, reconfigurer l'URL (`https://www.phyto-benin.com/api/whatsapp-webhook`) et le token (`gse-phyto-2026`), cliquer Vérifier et enregistrer, puis S'abonner sur le champ messages. Aucun changement de code nécessaire.

[2026-07-02] | QA de l'admin bloquée en local : la session Supabase (token dans localStorage) de l'utilisateur est sur la prod (www.phyto-benin.com), pas sur localhost:3000 — donc localhost/admin affiche l'écran de connexion et je ne saisis jamais de mot de passe moi-même. Perte de temps à démarrer un dev server et tenter un handoff/login local. | Pour toute QA navigateur de l'admin : déployer d'abord (commit + push), puis tester sur https://www.phyto-benin.com/admin où l'utilisateur est déjà connecté dans son Chrome. L'utilisateur pilote via l'extension MCP claude-in-chrome (navigateur nommé « gse »), pas le dev local. Ne jamais entrer/valider ses identifiants à sa place.

[2026-07-03] | QA d'un `<select>` React en prod via MCP : mon script cherchait la carte en remontant le DOM sur 6 niveaux et vérifiant `textContent.indexOf('Glory')` — à 4 niveaux l'ancêtre était tout le kanban board (qui contient « Glory »), donc le script a ciblé la 1re carte au lieu de la bonne (no-op sur une carte déjà au bon statut). | Pour piloter un `<select>` React par JS (set `.value` + `dispatchEvent(new Event('change',{bubbles:true}))`), scoper la recherche de la carte à 2 niveaux max (bouton→carte), jamais jusqu'au conteneur/board qui agrège le texte de toutes les cartes. Toujours vérifier après coup que les compteurs de colonnes ont bien changé (sinon on a agi sur la mauvaise carte).

[2026-07-03] | Le bouton « Traiter » un lead ne persistait pas : il retirait le lead de l'affichage mais l'`UPDATE` ne passait jamais → le lead revenait au rechargement. Cause : la table `leads` a RLS activé (migration 20260617) mais **aucune policy** → tout accès direct client-side (`db.from("leads").update/select` depuis page.js avec la session anon) est **refusé silencieusement** (pas d'erreur, 0 ligne affectée). L'admin voyait quand même les leads car la lecture passe par `/api/crm-data` côté serveur en `service_role` (bypass RLS). | Sur une table avec RLS activé sans policy, ne JAMAIS écrire/lire directement depuis le client (page.js) — router via une action `/api/crm-data` (service_role + verifyAdmin + Bearer token), conformément au standard projet « toute interaction Supabase passe par les routes API ». Symptôme typique d'un RLS bloquant : l'UI change puis l'état revient au reload, sans erreur console. Vérifier les policies dans supabase/migrations avant de débugger côté front.

[2026-07-03] | Même piège que le select : mon script JS de QA a re-ciblé la mauvaise carte car il remontait jusqu'à profondeur 2 = le conteneur des leads (qui agrège tous les noms), et a cliqué le 1er bouton « Traiter » (BIA Bénin) au lieu de Test QA Claude. | Pour cliquer un bouton dans une liste via JS, scoper la recherche du nom à la LIGNE immédiate (profondeur 1, le parent direct du bouton), jamais 2+ (qui atteint le conteneur listant tous les items). Vérifier ensuite quel item a réellement bougé.

[2026-07-03] | RÉCIDIVE du même bug sur une action DESTRUCTIVE : script JS de nettoyage cherchait la ligne « TEST Continuité » en remontant 5 niveaux → a matché le conteneur de toute la liste clients → a cliqué le mauvais bouton corbeille → a failli supprimer un vrai client (LOHATODE). Sauvé uniquement parce que la cible réelle était un client de test. | INTERDICTION de scripter (JS/coord) une action DESTRUCTIVE (supprimer client/devis/lead) en ciblant par nom via remontée DOM. Pour toute suppression : soit clic par COORDONNÉES après avoir vu l'élément exact à l'écran, soit récupérer l'ID précis via l'API et appeler l'endpoint de suppression avec cet ID — jamais une heuristique DOM. Et ne jamais neutraliser confirm() sur une suppression sans être certain à 100% de la cible. Vérifier le décompte avant/après.

[2026-07-10] | Le spec des notes vocales affirmait qu'un route handler Next.js "lit le body brut sans limite de 4 Mo" — vrai pour le body-parser Next mais FAUX au niveau plateforme : Vercel rejette toute requête POST > ~4,5 Mo avec un 413 AVANT d'exécuter le handler. L'UI annonçait 15 Mo × 5 fichiers en base64 (inflation ~33%) → un body rejeté, et `await res.json()` throwait sur le corps non-JSON du 413 → erreur illisible pour l'utilisateur. | Pour tout upload base64 POSTé en JSON à une route Vercel : garder le body total sous ~4,3 Mo (plafond par fichier bas + garde-fou sur la SOMME des base64 côté client AVANT le fetch), et ne jamais appeler `res.json()` sans try/catch — sur `!res.ok` prévoir un message de secours (`Erreur serveur (${res.status})`). Les gros fichiers doivent passer par Supabase Storage (URL) comme les photos, pas inline. Cette limite est invisible en test local (elle n'existe que sur Vercel), donc à anticiper au design.

[2026-07-11] | « + Nouveau client » (admin) créait bien le client ET un devis brouillon, mais le devis n'apparaissait jamais dans le dashboard CRM (client affiché « sans devis », absent même après F5, aucune erreur). Cause : `creerBrouillonDevis` (/api/create-client) ET `creerNouveauDevisClient` (page.js) insèrent le devis SANS définir `crm_statut` → NULL, or le dashboard filtre `d.crm_statut !== null` (crm-data/route.js:88). Le devis existait en base mais était filtré à l'affichage. `crm_statut` est nullable sans défaut (migration 20260527) ; le backfill de cette migration n'a touché que les lignes existantes, pas les futurs inserts. Piège aggravé par `creerBrouillonDevis` qui avale l'erreur d'insert (console.error seulement). | Tout insert de devis destiné au dashboard CRM DOIT définir `crm_statut` (ex. 'contact' pour un brouillon), comme le fait déjà l'action `add_client`. Quand une colonne nullable sert de filtre d'affichage serveur, ne jamais l'omettre à l'insertion. Symptôme typique « créé mais invisible, persistant après F5, sans erreur » = filtre serveur sur une colonne non renseignée, PAS un problème de sauvegarde ni de RLS (service_role bypasse RLS). Vérifier les `.filter(...)` du endpoint de lecture avant de suspecter l'écriture.

[2026-07-11] | Bouton « Créer le devis → » (fiche client, creerNouveauDevisClient) : rien ne se passait au clic, aucune erreur visible. Cause : insert DIRECT côté client `db.from("devis").insert(...)` sur une table RLS + appel au RPC `db.rpc("generate_devis_numero")` absent de toute migration. L'insert direct (session anon) est soumis à RLS et/ou le setMsg d'erreur n'est pas visible dans cette vue → échec silencieux. Viole le standard projet (coding-standards.md : « toute interaction Supabase passe par les routes API, jamais db.from direct »). | Ne jamais créer/modifier un devis (ou toute table RLS) par un `db.from(...).insert/update` direct depuis page.js. Router via une action /api/crm-data (service_role bypass RLS + verifyAdmin + Bearer token), générer le numéro avec crypto.randomUUID (jamais generate_devis_numero), définir crm_statut, et TOUJOURS vérifier res.ok pour remonter l'erreur. Ajout de l'action `add_devis`. Symptôme « clic sans effet, pas d'erreur » sur une écriture = écriture directe RLS qui échoue + erreur non affichée dans la vue courante.

[2026-07-11] | Bouton « ✏️ Modifier devis » depuis le tableau de bord d'un client (vue "devis-client") : rien ne s'affichait au clic. Cause : `ouvrirEditionDevis` faisait bien `setEditingDevis(d)`, mais le formulaire d'édition `renderFormDevis()` n'est monté QUE par `renderVueDevis()` (vue === "devis"). Depuis n'importe quelle autre vue (devis-client, pipeline), l'état changeait mais le formulaire n'était jamais dans l'arbre React → invisible. `creerNouveauDevisClient` fonctionnait car il faisait déjà `setVue("devis")` après. | Quand un bouton d'action ouvre un formulaire/modal rendu conditionnellement à une seule vue, l'ouvreur doit AUSSI basculer sur cette vue (`setVue("devis")`), pas seulement set l'état. Vérifier où le composant est effectivement monté dans l'arbre (grep de l'appel `renderXxx()`), pas seulement où l'état est déclaré. Symptôme « clic sans effet, pas d'erreur » sur une OUVERTURE de modal = état positionné mais composant non monté dans la vue courante.

[2026-07-11] | « Doublon » signalé sur BIA GROUPE Direction Générale dans l'admin : le nom s'affichait deux fois (« BIA GROUPE Direction Générale — BIA GROUPE Direction Générale »). Ce n'était PAS deux lignes en base (une seule ligne clients) mais un bug d'affichage : le champ `entreprise` contenait la même valeur que `nom`, et le rendu concatène `nom + " — " + entreprise` (~15 endroits dans page.js). 3 clients touchés (BIA Direction Générale, BIA Bohicon, Boulangerie La Manne Dorée). | Avant de croire à un doublon de lignes, vérifier en base (`select` groupé) : un nom affiché en double vient souvent d'un champ redondant (entreprise == nom) concaténé, pas de deux enregistrements. Corrigé en (1) vidant `entreprise` en base quand elle == `nom`, et (2) guard à l'enregistrement (`sauvegarderClient`) : `entreprise` vidée si trim/lower identique au `nom` avant tout insert/update. Penser à ce guard dès qu'un champ optionnel duplique un champ obligatoire affiché à côté.

[2026-07-11] | Utilisateur voyait « deux dossiers » par client BIA et galérait à retrouver un devis enregistré. Cause : `creerBrouillonDevis` créait un devis vide « À définir / 0 FCFA / brouillon » à CHAQUE ajout de client → faux 2e dossier ; + le CRM ouvrait sur Pipeline (pas la liste Devis) → devis introuvable → l'utilisateur le recréait (doublons). PIÈGE MAJEUR à la suppression : le brouillon « vide » de Bohicon portait en réalité 1 rapport_visite + 1 intervention (l'utilisateur avait fait le rapport sur le brouillon, seul « dossier » dispo à l'ajout). Le supprimer aurait détruit du vrai travail. | (1) Ne pas créer de devis fantôme à l'ajout d'un client — le devis se crée explicitement (retiré les 2 appels creerBrouillonDevis dans create-client). (2) Ouvrir le CRM sur la liste Devis (vueInitiale="devis"), filtre "tous", pour retrouver un devis facilement. (3) RÈGLE SUPPRESSION renforcée : avant de supprimer un devis « vide/brouillon », TOUJOURS vérifier les tables rattachées (interventions, rapports_visite, rapports_intervention, certificats, fiches_passage, contrats, paiements) via devis_id. Si des lignes existent, RÉASSIGNER (update devis_id vers le vrai devis du même client) puis supprimer — jamais supprimer en cascade du travail réel. « Vide » à l'écran ≠ vide en base.

[2026-07-11] | `npx supabase db push` refusait une nouvelle migration : « Found local migration files to be inserted before the last migration on remote ». Cause : le fichier `20260711000000_devis_lignes` entrait en COLLISION de timestamp avec une migration déjà appliquée (`20260711000000_backfill_crm_statut_null`), et une migration `20260711010000` postérieure était déjà appliquée. Supabase ordonne par version (préfixe timestamp) ; un nouveau fichier avec une version ≤ la dernière appliquée (ou en doublon) est « à insérer avant » et bloqué sans `--include-all`. Les outils MCP `apply_migration`/`execute_sql` renvoient « You do not have permission » (jeton MCP lecture seule) → inutilisables pour le DDL. | Nommer toute nouvelle migration avec un timestamp STRICTEMENT SUPÉRIEUR à la dernière migration distante (vérifier `npx supabase migration list`) et unique. En cas de blocage, renommer avec un timestamp postérieur (ex. `20260711020000`) plutôt que `--include-all` (qui pousserait d'autres migrations locales). Le DDL passe par `npx supabase db push` (CLI), pas par les outils MCP Supabase (lecture seule sur ce projet).

[2026-07-11] | « Déplacer vers → Devis envoyé » dans le pipeline ne faisait « rien » sur BIA Bohicon. En réalité `deplacerCarte` avait bien persisté `crm_statut='devis'`, MAIS la carte ne bougeait pas de colonne. Cause : `colUnifiee(c)` (renderVuePipelineUnifie) classe par PRIORITÉ D'EXÉCUTION avant l'étape commerciale — dès que `parcours.visite.done` (ou fiche/cert/intervention) est vrai, la carte est verrouillée dans la colonne d'exécution (Visite/Intervention/…), ce qui écrase `if (c.statut === "devis")`. Le `parcours.visite.done=true` venait du nettoyage précédent (réassignation du rapport de visite au vrai devis, où j'avais posé le marqueur). Le déplacement commercial devenait donc un no-op VISUEL silencieux. | Quand un déplacement de carte pipeline « ne fait rien », vérifier `parcours` du devis en base AVANT de suspecter l'écriture : `colUnifiee` fait primer l'exécution (parcours.*.done, fiches_passage, certificats) sur `crm_statut`. Pour faire reculer une carte vers une étape commerciale, il faut vider/retirer le marqueur de parcours correspondant (le rapport_visite en tant que ligne reste, seul le flag kanban est retiré). Attention : marquer `parcours.visite.done` lors d'un fix de données déplace la carte hors des colonnes commerciales.

[2026-07-13] | RÉCIDIVE signalée par l'utilisateur : usage de tirets cadratins (—) comme ponctuation dans un rendu livré (plaquette commerciale), alors qu'il avait déjà demandé de ne plus en voir. | INTERDICTION du tiret cadratin/demi-cadratin (— et –) comme signe de ponctuation dans TOUT rendu livré à l'utilisateur (documents Word/PDF, plaquettes, notes, tableaux) ET dans les réponses en chat. Remplacer par : virgule, deux-points, point, ou parenthèses. Les traits d'union à l'intérieur des mots composés (ré-intervention, multi-sites, agro-industrie, phyto-benin.com) restent légitimes. Vérifier avant livraison avec un grep sur "—" et "–" dans le texte visible.

[2026-07-11] | Refonte pipeline (le point précédent était la cause) : la colonne était RECALCULÉE (`colUnifiee`) depuis crm_statut + parcours + existence de fiches/certificats → couplage implicite, déplacements « sans effet », incohérences. | Désormais SOURCE DE VÉRITÉ UNIQUE = colonne `devis.etape` (prospect/devis/relance/converti/visite/intervention/certificat/encaissement/cloture/perdu). `colUnifiee(c)` renvoie juste `devisMap[c.id].etape || etapeParDefaut(crm_statut)`. `etape` est écrit à TOUS les points : action `move` (menu unifié + bouton Avancer→, dérive crm_statut + parcours cohérent), `saveParcours` (dérive via `etapeFromParcours`), `creerDevis` (→'devis' sans rétrograder), création certificat/fiche (`avancerEtapeMin`, forward-only). Toute nouvelle action qui fait progresser un devis DOIT mettre `etape` à jour, sinon la carte ne bouge pas. `parcours.encaissement.done` reste le seul flag critique lu par les Finances (tenu synchro par move idx>=8 et saveParcours). Migration 20260711030000 + backfill one-shot depuis l'ancienne logique colUnifiee.

[2026-07-18] | Devis admin : le récap affichait « Total brut : 115 687 FCFA » (somme des lignes) mais « Prix de base : 75 901 FCFA », et c'est ce dernier qui servait à la remise, au montant net, aux frais FedaPay et à ce qui était enregistré/facturé — écart de 39 711 FCFA sous-facturés. Cause : `formDevis.montantBrut` est un champ d'état SÉPARÉ du total des lignes. Il n'était resynchronisé que dans `setLigne` (édition d'une ligne) et à la suppression d'une ligne. `ouvrirEditionDevis` l'initialisait avec `d.montant_net || d.montant_total`, c'est-à-dire le montant APRÈS remise de la version précédente : si l'utilisateur rouvrait un devis et ne touchait aucune ligne (il ne changeait que la remise ou le mode de transmission), la base restait figée sur l'ancien montant pendant que les lignes en affichaient un autre. Effet secondaire : la remise se ré-appliquait en cascade à chaque réouverture, puisque la base repartait du net déjà remisé. | Quand une valeur est DÉRIVABLE d'une autre (prix de base = somme des lignes), ne jamais la stocker dans un second champ d'état resynchronisé à la main dans quelques handlers : la dérivation doit être recalculée au rendu ET à l'enregistrement. Helper unique `baseDevis(form)` = `totalLignes(form.lignes)` si > 0, sinon la saisie manuelle de secours ; utilisé par le bloc récap ET par `creerDevis`. `ouvrirEditionDevis` initialise `montantBrut` depuis les lignes, jamais depuis `montant_net` (montant post-remise = base fausse). Symptôme typique : deux totaux différents affichés dans le même formulaire — le montant facturé est toujours celui du calcul, pas celui affiché en gros.

[2026-07-18] | « Page blanche à la sauvegarde d'un devis » : j'ai diagnostiqué un problème de défilement (formulaire démonté → page raccourcie → utilisateur bloqué dans le vide) et écrit le correctif AVANT de vérifier le symptôme. L'utilisateur a répondu « tout est blanc même en haut », puis, deux échanges plus tard, la vraie information : « c'est quand je change le nom du devis qu'il apparaît en blanc dans mes téléchargements ». Il ne s'agissait ni de React ni de la page admin, mais du PDF exporté depuis la fenêtre d'impression. Deux tours perdus à lire du code de rendu sans rapport. | « Page blanche » est ambigu et ne désigne presque jamais ce qu'on croit : AVANT de lire du code, faire préciser QUELLE surface est blanche (la page de l'app / une fenêtre ouverte / un fichier téléchargé / un aperçu), et si le contenu revient en remontant. Une question fermée à choix multiples coûte un tour et évite d'en perdre trois. Ne jamais écrire un correctif tant que le symptôme n'est pas localisé.

[2026-07-18] | Le PDF d'un devis sortait vide quand l'utilisateur renommait le fichier dans la boîte « Enregistrer au format PDF » de Chrome ; avec le nom proposé par défaut, le PDF était correct. Les fenêtres de documents (devis, certificats, fiches, rapports) sont ouvertes par `window.open("", "_blank")` puis `document.write(html)` : le document est un about:blank sans URL réelle, donc sans source rechargeable pendant que la boîte d'enregistrement reste ouverte plusieurs secondes. Le contrat, lui, passe par une vraie URL (`/api/generate-contract`) et n'a jamais eu le problème. | Pour tout document destiné à être imprimé ou enregistré en PDF, ouvrir une URL blob (`URL.createObjectURL(new Blob([html], {type:"text/html"}))`) plutôt que `document.write` sur un about:blank : le document a alors une vraie source. Révoquer l'URL en différé (60 s), jamais immédiatement. Et soigner le `<title>` : c'est lui que Chrome propose comme nom de fichier, donc le rendre directement exploitable (helper `nomFichierDoc` : pas de tiret cadratin, pas de `/ \ : * ? " < > |`, espaces en underscores) pour que l'utilisateur n'ait pas à le retaper. Restent à migrer : les 6 autres `window.open('', '_blank')` (certificats, fiches de passage, rapports de visite et d'intervention).

[2026-07-19] | L'utilisateur a demandé de « partir sur » la transformation d'un devis en contrat, croyant la fonctionnalité inexistante. Elle était complète depuis longtemps (modal `renderContratModal`, analyse IA, `/api/generate-contract`, réimpression) mais son unique point d'entrée, le bouton « 📄 Contrat », se trouvait dans `renderVueDevisClient` (tableau de bord d'un client), une vue qu'on n'atteint qu'en cliquant « 📊 Dashboard » depuis la liste des devis. En travaillant dans le pipeline ou la liste, elle était donc introuvable. | Avant de développer une fonctionnalité demandée, TOUJOURS grep les points d'entrée existants (`grep -n "setXxxModal"`) : sur ce projet, plusieurs fonctionnalités complètes sont enterrées dans une seule vue. Et à l'inverse, quand on ajoute une action liée à une étape du parcours, la rendre accessible depuis la vue où l'utilisateur travaille réellement à cette étape (le pipeline), pas seulement depuis une vue de détail. Une fonctionnalité invisible équivaut à une fonctionnalité absente : l'utilisateur redemande son développement.

[2026-07-19] | Confirmation utilisateur : le passage à une URL blob corrige bien le PDF vide au renommage. Extension aux 6 autres fenêtres de documents (certificats ×2, fiche de passage ×2, rapports de visite et d'intervention). Au passage, les rapports de visite et d'intervention n'avaient AUCUN identifiant dans leur `<title>` (« Rapport de visite — GSE »), ce qui obligeait à renommer systématiquement, donc à déclencher le bug à chaque impression. | Les helpers `nomFichierDoc` et `ouvrirDocImprimable` sont au niveau MODULE (pas dans SectionClientsDevis) : les générateurs `buildCertificatHtml` / `buildFichePassageHtml` / `buildRapportVisiteHtml` / `buildRapportIntervHtml` sont des fonctions top-level et n'ont pas accès à la portée du composant. Tout nouveau document imprimable doit passer par `ouvrirDocImprimable(html, largeur, hauteur)` et poser un `<title>` construit avec `nomFichierDoc(type, identifiant, client, date)` : jamais un titre générique sans identifiant, sinon l'utilisateur renomme à chaque fois.

[2026-07-19] | Le prompt de analyze-contract lisait trois champs inexistants (devis.montant, devis.prestations, et une requête d'historique sur montant qui échouait en 400). L'IA proposait donc un prix de contrat sans connaître le montant du devis, et tout client apparaissait comme nouveau, si bien que la règle de remise fidélité n'a jamais servi. Aucune erreur visible : les requêtes en échec produisaient des valeurs par défaut (« Non précisé », « 0 devis antérieurs ») indiscernables de faits. Vérifié après correction sur le devis de Jean Folly : l'IA voit désormais 104 118 FCFA et 578,433 m², contre « Non précisé » sur les deux auparavant. | Un prompt qui interpole des champs de base de données doit être vérifié champ par champ contre le schéma réel, comme du code. Et toute requête annexe en échec doit écrire « indisponible » dans le prompt, jamais une valeur par défaut : une absence d'information présentée comme un fait produit une recommandation fausse sans jamais lever d'erreur.

[2026-07-19] | Deux défauts du plan d'implémentation n'ont été trouvés qu'en exécutant le code contre les vrais services, jamais par les tests unitaires ni par la relecture. (1) maxOutputTokens à 2048 sur la phase questions : gemini-2.5-flash dépense des tokens de sortie en raisonnement avant d'émettre son JSON, la réponse sortait tronquée 3 fois sur 3 et la route répondait 500. (2) Le bouton déclenchant la phase questions avait pour condition un état renseigné seulement APRÈS un appel API, alors que les seuls chemins qui le renseignaient étaient ce bouton lui-même ou l'analyse qui masque le formulaire : la fonctionnalité entière était du code mort côté interface, et compilait parfaitement. | Un plan validé et des tests verts ne prouvent pas qu'une fonctionnalité est atteignable ni qu'elle fonctionne. Pour toute fonctionnalité passant par une IA ou par un état React conditionnel : (a) l'appeler réellement au moins une fois contre le vrai service avant de la déclarer terminée, un budget de tokens ne se déduit pas de la longueur attendue de la réponse ; (b) dérouler explicitement le premier rendu, avant tout appel réseau, pour vérifier que le point d'entrée est visible. Quand l'interface a besoin d'une information que le composant charge déjà en mémoire (ici l'état rapportsVisite), la calculer localement plutôt que de l'attendre d'une réponse API.

[2026-07-29] | Contrat généré pour un devis « Désinsectisation » seule (M. HOUNSOU) : le contrat affichait quand même une partie « Dératisation : vérification et rechargement des stations » et une ligne entière « Contrôle des stations à rongeurs ». Cause : `app/api/generate-contract/route.js` codait EN DUR le détail de l'Article 3 (traitement insecticide + dératisation) et ajoutait `controleRow` dès que `controles > 0` (défaut 8, envoyé par le front pour tout contrat trimestriel), sans jamais regarder la prestation réelle du devis. Le défaut ligne 45 valait même « Désinsectisation + Dératisation ». L'utilisateur l'a vu à la relecture ; sinon on facturait/engageait GSE sur une prestation non vendue. | Un document contractuel/commercial ne doit décrire QUE les volets réellement vendus, dérivés de la donnée source (`devis.prestation`), jamais un gabarit en dur. Dans generate-contract : booléens `aDeratisation`/`aDesinsectisation`/`aDesinfection` calculés depuis `prestationLabel` normalisé (NFD + suppression diacritiques), détail de l'intervention construit par `.filter(Boolean)`, et `controleRow` (stations à rongeurs) affiché seulement si `aDeratisation`. Le garde-fou est posé dans la ROUTE (source de vérité), pas dans le front, pour couvrir les deux chemins (IA et direct) quelle que soit la valeur `controles` envoyée. Défaut de prestation neutre (« Prestation sanitaire (voir devis) »), jamais un défaut qui invente une prestation. Vérifier tout gabarit de document contre la prestation avant livraison.

[2026-08-14] | QA prod : `/api/admin-auth` était la SEULE route admin sans `verifyAdmin`. GET exposait publiquement la liste des admins (emails = identifiants) et le journal ; POST `create_user`/`change_password`/`delete_user` étaient appelables sans jeton → n'importe qui pouvait s'ajouter admin ou réinitialiser le mot de passe du DG (prise de contrôle totale). Le front appelait les 5 endpoints sans en-tête Authorization. Même classe de faille que l'audit 2026-06-02, réapparue sur une route ajoutée après. | Toute route qui lit/écrit des données sensibles DOIT passer par `verifyAdmin(req)` (jeton Bearer + appartenance à `admin_acces`), y compris les GET de lecture. Seul `setup_first_admin` reste ouvert car auto-verrouillé (n'agit que si la table est vide). Le front doit envoyer `Authorization: Bearer <access_token>` sur CHAQUE appel admin (helper `authHeaders()`). Pour le bootstrap, exposer un simple drapeau `needsSetup` (table vide), jamais les données. Après ajout de toute nouvelle route API, vérifier en prod qu'un appel non authentifié renvoie 401, pas 200 avec données.

[2026-08-15] | Première intention pour le pointage fait/pas fait d'un passage de contrat : `db.from("interventions").update(...)` en direct depuis app/admin/page.js. Or `interventions` a RLS activé (migration 20260602) avec policy `interventions_admin` : l'écriture directe depuis le client viole le standard projet et le piège documenté à répétition (échec silencieux 0 ligne, revert au reload). Rattrapé en relisant lessons.md avant de déployer. | Toute écriture sur `interventions` (comme toute table RLS) passe par une action API : ici `set_passage_statut` dans /api/rh-data (service_role + verifyAdmin), appelée avec `authHeaders()`. rh-data est le propriétaire des écritures interventions (add/save/del_intervention) : y ajouter les nouvelles actions liées, pas les disperser. Réflexe : avant tout `db.from(...).update/insert/delete` dans page.js, vérifier la RLS de la table et router par l'API.

[2026-08-17] | Annonces recrutement : après avoir ajouté des colonnes (pourquoi_postuler, futur_employeur…), les blocs ne s'affichaient pas en prod alors que les données étaient bien en base. Cause : DEUX chemins lisent les offres avec leur PROPRE `.select(...)` — `app/api/offres/route.js` (que j'avais mis à jour) ET le `getOffres` interne de `app/recrutement/page.tsx` (que j'avais oublié, il listait encore les 6 colonnes d'origine). La page recevait les nouveaux champs en `undefined`. | Quand plusieurs endroits chargent la même table avec une liste de colonnes explicite, les mettre TOUS à jour en même temps (grep la table, ex. `grep -rn "from(\"offres_emploi\")"`). Symptôme « données en base mais absentes à l'écran, sans erreur » sur un nouveau champ = un `.select()` qui ne liste pas la colonne quelque part. Éviter les selects de colonnes dupliqués divergents ; à défaut, vérifier chaque chemin de lecture.

[2026-08-17] | Sur le module recrutement, j'ai rédigé et publié le CONTENU de l'annonce commerciale (missions, profil, « Temps plein + commissions ») sans demander à Kabir si ça correspondait à son contexte réel (rémunération, missions exactes). L'utilisateur l'a relevé. En plus, l'admin ne permettait pas de MODIFIER une offre (seulement ajouter/fermer/supprimer), donc il ne pouvait même pas corriger mon texte. | Ne jamais rédiger un contenu métier destiné à être publié (annonce d'emploi, texte commercial, description de service, tarif) en inventant les détails du contexte de l'entreprise : soit demander les infos à l'utilisateur, soit poser un gabarit neutre clairement marqué « à compléter », jamais un contenu qui a l'air définitif. Et quand on crée une entité éditable par l'utilisateur (offre, article, service), fournir DÈS LE DÉPART le CRUD complet, l'édition incluse, pas seulement create/delete : sans « Modifier », l'utilisateur est bloqué avec mon contenu.

[2026-08-15] | Préparation auto du certificat : j'ai créé DEUX certificats (un 'derat' + un 'desinsect') quand le contrat vendait les deux prestations, alors que le système a déjà un type 'double' (migration 20260540) qui les regroupe en UN seul document (buildCertificatHtml gère 'double' avec les deux blocs matières). J'avais lu le schéma initial `CHECK type IN ('desinsect','derat')` sans voir la migration ultérieure qui ajoute 'double'. | Quand un document/représentation existe déjà pour un cas combiné, GREP tout le pipeline (générateur HTML + TOUTES les migrations, pas juste la table d'origine) avant de recréer la logique. Pour le certificat, lire `devis.prestation` et mapper : deux volets => un seul certificat type 'double' ; sinon 'desinsect' ou 'derat'. Une contrainte CHECK peut être élargie par une migration postérieure : toujours vérifier `grep -rn "ADD CONSTRAINT\|type_check\|double"` sur tout supabase/migrations, jamais se fier au seul CREATE TABLE.

[2026-08-15] | Le pointage fait/pas fait échouait toujours (« Échec de l'enregistrement du passage »). Cause : `togglePassage` appelait `authHeaders()`, un helper défini dans le composant EXTERNE (page Admin), alors que la fonction vit dans `SectionClientsDevis` (composant enfant qui reçoit `db` en prop). `authHeaders` n'y est pas dans le scope → ReferenceError, avalée par le `try/catch`, transformée en échec générique. Le build passait (identifiant libre résolu au runtime, pas au build). Symptôme trompeur : on croyait à un 401/erreur API alors que le fetch ne partait jamais. | app/admin/page.js contient DEUX composants : la page Admin externe (a `authHeaders`, `supabase`) et `SectionClientsDevis` (a `db` en prop = le même client). Dans `SectionClientsDevis`, récupérer le token via `db.auth.getSession()` (pattern déjà utilisé, ex. `tok2`), jamais `authHeaders()`. Avant d'appeler un helper dans une fonction de page.js, vérifier dans QUEL composant il est défini (les deux ne partagent pas leur scope). Un `try/catch` qui retombe sur un message générique masque une ReferenceError : en debug, logguer `e` avant de conclure à une erreur réseau/serveur.

[2026-08-15] | RÉCIDIVE tirets cadratins : usage de « — » comme ponctuation dans plusieurs réponses en chat, alors que la règle du 2026-07-13 l'interdit explicitement aussi dans le chat. | Avant d'envoyer une réponse, relire mentalement pour proscrire « — » et « – » comme ponctuation (chat ET rendus livrés). Remplacer par virgule, deux-points, point, parenthèses. La règle couvre le chat, pas seulement les documents.

[2026-08-22] | En fin de déploiement, j'ai donné à l'utilisateur l'URL technique `physto-benin.vercel.app` (codée en dur dans /deploy-gse et ui-qa) comme lien du site. Elle contient la faute de frappe d'origine du repo (« physto ») et n'est pas le domaine qu'il regarde. Réaction : « c'est quoi ça ». | Le site de production est TOUJOURS https://www.phyto-benin.com (jamais l'URL *.vercel.app dans un message à l'utilisateur). Les checks HTTP peuvent viser le domaine réel directement. URLs corrigées dans .claude/commands/deploy-gse.md et .claude/agents/ui-qa.md. Ne jamais présenter une URL vercel.app comme « le site » : donner le domaine.

[2026-07-29] | RÈGLE MÉTIER (tarification contrat). Le prix du contrat était fixé librement par l'IA (Gemini), non reproductible (320 000 puis 400 000 sur le même dossier, documenté dans le code) et sans lien garanti avec la rentabilité. Constaté sur le devis Hounsou (132 265 F ponctuel, contrat 115 000/trim proposé par l'IA sans justification chiffrée vérifiable). | Le prix du contrat de maintenance est DÉTERMINISTE, calculé dans le code (`offresContrat` dans lib/contrat-analyse.mjs), jamais par l'IA (qui ne produit plus que le texte). Modèle validé avec la direction : `prix/passage(durée) = max(plancher, taux(durée) × prix_ponctuel)`, prix_ponctuel = montant net du devis (prix d'UNE intervention = plafond), `TAUX_ENGAGEMENT` = {3:1.0, 6:0.95, 12:0.9}, plancher = `PLANCHER_PONCTUEL_PCT` (0.7) du ponctuel. Un passage sous contrat coûte TOUJOURS moins qu'une intervention ponctuelle et JAMAIS sous le plancher. Le contrat présente les 3 formules (3/6/12 mois) avec cases à cocher (Article 4) : le client coche, GSE applique ; 12 mois mis en avant. Un prix négocié prime encore sur l'offre 12 mois (engagement pris) mais déclenche un point d'attention s'il passe sous le plancher. Ne jamais laisser une IA fixer un montant contractuel.

[2026-09-05] | Nouvel export PDF (liste prospects) ouvrait une fenêtre blanche : GSE_DOC_STYLES commence par `<style>` mais NE ferme PAS la balise — c'est l'appelant qui doit ajouter `</style>`. J'avais mis `GSE_DOC_STYLES + '</head>'` sans `</style>`, donc tout le body était avalé comme du CSS. Le bug est passé inaperçu car mon aperçu de test avait, lui, un `</style>` correct. | Toujours écrire `GSE_DOC_STYLES + '</style></head><body>'` (jamais GSE_DOC_STYLES seul). Pour tester un document imprimable, rendre le HTML RÉELLEMENT produit par la fonction (extraire la string), pas une copie reconstruite à la main.
